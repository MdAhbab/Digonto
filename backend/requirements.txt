# Digonto backend. Python 3.12.
# Pinned to minor versions: a hackathon deploy that breaks on a transitive
# upgrade the night before judging is a self-inflicted wound.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20

# Storage
aiosqlite==0.20.0
redis==5.2.1
qdrant-client==1.12.2

# Background work.
#
# No `arq`. The pin was here from an earlier design and nothing under
# app/workers ever imported it: the worker process runs its consumers as plain
# asyncio tasks over `EventBus.consume` (Redis Streams, at-least-once, idempotent
# via `applied_events`) with APScheduler for cron timing. Installing a second,
# incompatible job queue next to the bus this build already has would be pure
# surface area, so the pin is dropped rather than adopted. See backend/backend.md
# section 1.
apscheduler==3.11.0

# Security
argon2-cffi==23.1.0
pyjwt==2.10.1
cryptography==44.0.0

# HTTP and crawling
httpx==0.28.1
selectolax==0.3.27
tenacity==9.0.0

# Documents. pypdfium2 ships a self-contained pdfium wheel for every platform
# we deploy on, so rasterising page 1 of a PDF needs no system package (no
# poppler, no ghostscript). The vault service needs it because the vision
# agents (Bicharok, Dalil) and the Gemma field extraction all take an image,
# never a PDF.
pypdfium2==5.12.1

# Text
rank-bm25==0.2.2
python-ulid==3.0.0
regex==2024.11.6

# Observability
prometheus-client==0.21.1
structlog==24.4.0

# MCP servers (app/mcp/). Pinned to 1.12.4 specifically: newer mcp releases
# pull in an sse-starlette that in turn drags starlette to 1.x, which breaks
# fastapi==0.115.6's own starlette<0.42 pin. 1.12.4 is the newest version
# whose declared dependency floors (pydantic>=2.8, starlette>=0.27,
# uvicorn>=0.23.1, httpx>=0.27) are already satisfied by the pins above, so
# installing it changes nothing else in this file.
mcp==1.12.4

# Dev
pytest==8.3.4
pytest-asyncio==0.25.0
